---
title: "Стандарты OCI и среды выполнения контейнеров"
description: "Спецификации OCI, разделение на low-level и high-level рантаймы, runc, containerd, CRI-O, роль shim и интерфейс CRI."
sidebar:
  order: 5
---

К 2015 году вокруг контейнеров сложилась типичная для молодых технологий проблема — фрагментация. Docker задавал де-факто стандарт, но его формат образа и способ запуска были привязаны к одной реализации. Параллельно появлялись альтернативы (например, CoreOS с форматом appc и рантаймом rkt), и индустрия рисковала расколоться на несовместимые экосистемы: образ, собранный одним инструментом, мог не запуститься в другом, а оркестраторам пришлось бы поддерживать зоопарк форматов. Чтобы этого избежать, в июне 2015 года под эгидой Linux Foundation была учреждена **Open Container Initiative (OCI)** — открытая структура управления для выработки отраслевых стандартов на форматы и среды выполнения контейнеров. Docker передал в OCI свой низкоуровневый рантайм **runc**, который стал эталонной (reference) реализацией.

Ключевая идея OCI — разделить «что такое контейнер» (спецификации) и «кто его запускает» (рантаймы), а сами рантаймы расслоить на уровни. Благодаря этому образ, собранный в одном инструменте, корректно запускается в любом OCI-совместимом рантайме, а Kubernetes может менять рантайм под собой, не переписывая остальную систему.

## Три спецификации OCI

OCI стандартизирует три независимых, но дополняющих друг друга артефакта.

| Спецификация | Что описывает | Где применяется |
|---|---|---|
| **image-spec** | Формат образа: манифест, слои файловой системы, конфигурация (команда, аргументы, переменные окружения), дайджесты | Сборка и хранение образов — подробнее в [/containerization/images/](/containerization/images/) |
| **runtime-spec** | Формат «бандла» (filesystem bundle) и правила его запуска: жизненный цикл контейнера, операции create/start/kill/delete | Низкоуровневый запуск контейнера |
| **distribution-spec** | HTTP API реестра (registry) для push/pull контента по дайджестам и тегам | Передача образов между реестром и хостом |

**image-spec** описывает, как устроен образ на диске и в реестре: манифест перечисляет слои и ссылается на объект конфигурации, слои представлены как tar-архивы с разностью файловой системы, а всё содержимое адресуется по криптографическому дайджесту (content-addressable). Это та самая модель слоёв, которая разбирается в разделе [/containerization/images/](/containerization/images/).

**runtime-spec** определяет, что рантайм получает на вход не образ, а уже распакованный **OCI bundle** — и описывает, как из него запустить процесс. **distribution-spec** (v1.0 — 2021 год) формализует API реестра, который раньше существовал как протокол Docker Registry HTTP API V2.

### Структура OCI bundle

Бандл — это каталог на локальной файловой системе, из которого low-level рантайм создаёт контейнер. Он состоит ровно из двух частей: обязательного файла `config.json` в корне каталога и корневой файловой системы контейнера (по умолчанию каталог `rootfs`, путь задаётся полем `root.path`).

```mermaid
flowchart TD
    B["OCI bundle (каталог)"] --> C["config.json (конфигурация)"]
    B --> R["rootfs/ (корневая ФС)"]
    C --> C1["process: команда, env, cwd, uid/gid"]
    C --> C2["linux.namespaces: pid, net, mnt, ..."]
    C --> C3["linux.resources: cgroups-лимиты"]
    C --> C4["mounts: /proc, /sys, /dev и тома"]
    C --> C5["linux.seccomp, capabilities"]
    R --> R1["/bin, /etc, /usr, ... приложения"]
```

`config.json` — это декларативное описание контейнера: какой процесс запустить, в каких namespaces его изолировать, какие cgroups-лимиты наложить, что и куда смонтировать, какой seccomp-профиль и набор capabilities применить (про namespaces и cgroups см. [/containerization/namespaces/](/containerization/namespaces/) и [/containerization/cgroups/](/containerization/cgroups/), про seccomp и capabilities — [/containerization/security/](/containerization/security/)). High-level рантайм распаковывает слои образа в `rootfs`, генерирует `config.json` и передаёт готовый бандл низкоуровневому рантайму.

## Уровни рантаймов: low-level и high-level

OCI-совместимые среды выполнения делятся на два уровня с разной зоной ответственности.

- **Low-level (OCI runtime)** — реализует именно runtime-spec: берёт готовый бандл и превращает его в живой процесс, создавая namespaces, настраивая cgroups, применяя seccomp и capabilities, после чего запускает целевой процесс как PID 1 контейнера. Он ничего не знает об образах, реестрах и сети.
- **High-level (container runtime)** — управляет образами (pull/push, хранение слоёв через OverlayFS), сетью, томами и жизненным циклом множества контейнеров, но сам контейнер не запускает: делегирует это low-level рантайму через стандартный интерфейс.

### Low-level рантаймы

- **runc** — эталонная реализация runtime-spec на Go. Напрямую вызывает системные вызовы Linux (`clone`/`unshare` для namespaces, запись в файловую систему cgroups) и запускает процесс. Большинство инсталляций используют именно его.
- **crun** — альтернатива на C от сообщества (Red Hat). Меньше по размеру и быстрее запускается за счёт отсутствия рантайма Go и более лёгкого процесса; полностью совместима с OCI runtime-spec.
- **Рантаймы усиленной изоляции** — **kata-runtime** (запускает каждый контейнер в облегчённой виртуальной машине с отдельным ядром) и **gVisor/runsc** от Google (перехватывает системные вызовы в пользовательском пространстве через собственное ядро-прокси). Они подключаются туда же, где ожидается OCI runtime, но дают границу безопасности уровня гипервизора. Подробнее о модели угроз и компромиссах — в [/containerization/security/](/containerization/security/); о грани между контейнерами и ВМ — в [/virtualization/containers-vs-vm/](/virtualization/containers-vs-vm/).

:::note
Low-level рантаймы взаимозаменяемы, потому что все они принимают один и тот же формат бандла. Сменить runc на crun или runsc обычно можно правкой одной строки в конфигурации high-level рантайма — без изменений в образах и приложениях.
:::

### High-level рантаймы

- **containerd** — демон, изначально выделенный из Docker и переданный в CNCF. Управляет полным жизненным циклом: образы, снапшоты файловой системы, передача в реестр, сеть (через плагины), задачи (tasks). Сам процесс контейнера запускает через runc.
- **CRI-O** — лёгкий рантайм от сообщества Kubernetes, специально заточенный под CRI. Делает только то, что нужно kubelet, и по умолчанию работает с crun или runc.

## Роль shim

Между high-level рантаймом и low-level рантаймом стоит **shim** (например, `containerd-shim-runc-v2`). Это критически важный посредник. После запуска контейнера shim становится его родительским процессом, а сам runc завершается — он нужен только на момент создания.

Зачем это:

- **Развязка от демона.** Контейнер не является дочерним процессом containerd. Поэтому containerd можно перезапустить или обновить, не убивая работающие контейнеры — их родителем остаётся shim, переживающий рестарт демона.
- **Сбор кода завершения и потоков ввода-вывода.** Shim держит открытыми stdout/stderr контейнера и его exit code, передавая их демону, когда тот снова доступен.
- **Развязка от управляющего процесса.** Один shim обслуживает один контейнер, изолируя сбои.

```mermaid
flowchart TD
    K["kubelet / Docker / nerdctl"] -->|gRPC| C["containerd (демон)"]
    C -->|создаёт| S["containerd-shim"]
    S -->|fork+exec| RC["runc"]
    RC -->|"создаёт namespaces + cgroups, exec"| P["Процесс контейнера (PID 1)"]
    S -. остаётся родителем .-> P
    RC -.->|завершается после старта| X["(runc выходит)"]
```

## CRI: интерфейс Kubernetes к рантайму

**Container Runtime Interface (CRI)** — это gRPC-интерфейс, через который **kubelet** (агент Kubernetes на узле) общается с рантаймом. kubelet выступает gRPC-клиентом и не зависит от конкретной реализации: эндпоинт рантайма задаётся флагом `--container-runtime-endpoint`. CRI определяет два сервиса — RuntimeService (создание pod sandbox, контейнеров, операции жизненного цикла) и ImageService (управление образами). Это и есть слой, который позволил Kubernetes убрать прямую зависимость от Docker: достаточно, чтобы рантайм реализовывал CRI.

CRI реализуют **containerd** (через встроенный CRI-плагин) и **CRI-O**. О том, как kubelet и узлы складываются в кластер, — в разделе [/containerization/orchestration/](/containerization/orchestration/).

:::tip
Начиная с Kubernetes 1.24 встроенная поддержка Docker через прослойку dockershim удалена. Современные кластеры используют containerd или CRI-O напрямую — это историческое следствие появления CRI.
:::

### Полная цепочка запуска в Kubernetes

Когда планировщик назначает под на узел, запрос проходит сверху вниз через все уровни:

```mermaid
sequenceDiagram
    participant K as kubelet
    participant CD as containerd
    participant SH as containerd-shim
    participant RC as runc
    participant P as Процесс контейнера
    K->>CD: CRI gRPC: CreateContainer / StartContainer
    CD->>CD: подготовка bundle (rootfs + config.json)
    CD->>SH: запуск shim для задачи
    SH->>RC: вызов runc create / start
    RC->>P: namespaces, cgroups, exec команды
    RC-->>SH: runc завершается
    SH-->>CD: статус и поток stdout/stderr
    Note over SH,P: shim остаётся родителем PID 1
```

Цепочка: **kubelet → CRI → containerd → containerd-shim → runc → процесс контейнера**. Каждый уровень добавляет ровно одну ответственность, и любой из них (рантайм, shim, low-level бинарь) можно заменить совместимым по стандарту.

## Инструменты для практики

Каждый уровень доступен напрямую через свой CLI — это полезно для отладки и понимания.

| Инструмент | Уровень | Назначение |
|---|---|---|
| `runc spec` / `runc run` | low-level | Сгенерировать заготовку `config.json` и запустить бандл вручную |
| `ctr` | containerd | Отладочный CLI containerd (образы, контейнеры, задачи) |
| `crictl` | CRI | Отладка через CRI-эндпоинт — «как видит рантайм kubelet» |

Минимальный эксперимент с эталонным рантаймом показывает, что контейнер — это всего лишь бандл и процесс:

```bash
# Подготовить rootfs из образа (см. /containerization/images/) и каталог бандла
mkdir -p mycontainer/rootfs
# Экспорт ФС образа в rootfs опускаем для краткости

cd mycontainer
runc spec                 # создаёт шаблонный config.json
runc run mycontainerid    # запускает контейнер из бандла
```

`runc spec` создаёт готовый `config.json` с разумными значениями по умолчанию (namespaces, базовые mounts, seccomp), а `runc run` исполняет бандл напрямую — без containerd, Docker и Kubernetes. Именно эту работу под капотом выполняет вся остальная цепочка. О том, как высокоуровневые инструменты прячут эти детали за удобным интерфейсом, — в разделе [/containerization/docker/](/containerization/docker/).

:::caution
`ctr` и `crictl` — инструменты отладки, а не повседневной эксплуатации. `ctr` работает поверх containerd напрямую и не видит метаданные Kubernetes; `crictl` смотрит через CRI и показывает поды и контейнеры так, как их понимает kubelet. Для прикладных задач используйте `kubectl` или `nerdctl`.
:::

## Задания

### Задание 1. Три спецификации и разделение ответственности

Объясните, зачем OCI разделили стандарт на три независимые спецификации (image-spec, runtime-spec, distribution-spec) вместо одного монолитного описания «контейнера». Какую конкретную проблему 2015 года это решает? Для каждой спецификации укажите, на каком этапе жизни контейнера она работает.

<details>
<summary>Решение</summary>

Проблема 2015 года — **фрагментация**: Docker задавал де-факто стандарт, но формат образа и способ запуска были привязаны к одной реализации. Параллельно появлялись альтернативы (appc/rkt от CoreOS), и образ из одного инструмента мог не запуститься в другом, а оркестраторам пришлось бы поддерживать зоопарк форматов.

Ключевая идея OCI — разделить «что такое контейнер» (спецификации) и «кто его запускает» (рантаймы). Три спецификации развязывают **независимые этапы**, чтобы каждый можно было менять, не трогая остальные:

| Спецификация | Этап жизни контейнера | Что описывает |
|---|---|---|
| **image-spec** | сборка и хранение | формат образа: манифест, слои-tar с разностью ФС, объект конфигурации, адресация по дайджесту (content-addressable) |
| **distribution-spec** | передача между реестром и хостом | HTTP API реестра для push/pull контента по дайджестам и тегам (формализован в v1.0 2021 г., раньше — Docker Registry HTTP API V2) |
| **runtime-spec** | низкоуровневый запуск | формат OCI bundle и правила его запуска: жизненный цикл, операции create/start/kill/delete |

Эффект разделения: образ, собранный одним инструментом, корректно запускается в любом OCI-совместимом рантайме, а Kubernetes может менять рантайм под собой, не переписывая остальную систему.

</details>

### Задание 2. Что произойдёт, если убрать shim

В цепочке `kubelet → CRI → containerd → containerd-shim → runc → процесс` рассмотрите гипотетическую упрощённую схему без shim, где `runc` запускается напрямую как дочерний процесс демона containerd и не завершается, оставаясь родителем процесса контейнера.

а) Что случится с работающими контейнерами при перезапуске/обновлении containerd в этой схеме и почему?
б) Какие ещё функции shim будут потеряны?
в) Как реальная схема с shim решает эти проблемы?

<details>
<summary>Решение</summary>

**а) Контейнеры умрут при рестарте демона.** Если процесс контейнера — потомок containerd (напрямую или через незавершающийся runc как дочерний процесс демона), то остановка демона убивает всё его поддерево процессов. Обновить или перезапустить containerd без простоя контейнеров будет невозможно.

**б) Будут потеряны:**

- **Сбор exit code и потоков ввода-вывода.** Некому держать открытыми stdout/stderr контейнера и его код завершения, пока демон недоступен; данные о завершившемся контейнере потеряются в окне рестарта.
- **Изоляция сбоев.** Один управляющий процесс на все контейнеры означает, что его падение затрагивает все контейнеры сразу.

**в) Реальная схема с shim:**

- После запуска контейнера shim становится его **родительским процессом**, а сам `runc` завершается — он нужен только на момент создания (создать namespaces/cgroups и сделать exec). Контейнер при этом **не является дочерним процессом containerd**.
- Поэтому containerd можно перезапустить или обновить, не убивая работающие контейнеры: их родителем остаётся shim, переживающий рестарт демона (**развязка от демона**).
- Shim держит открытыми stdout/stderr и exit code, передавая их демону, когда тот снова доступен.
- **Один shim обслуживает один контейнер** (`containerd-shim-runc-v2`), изолируя сбои (развязка от управляющего процесса).

</details>

### Задание 3. Замена low-level рантайма и границы изоляции

В кластере нужно для части нагрузок (недоверенный код) усилить изоляцию до уровня гипервизора, оставив остальное на стандартном рантайме.

а) Почему смена low-level рантайма (например, runc → crun, или runc → runsc/kata) обычно сводится к правке одной строки в конфигурации high-level рантайма и не требует пересборки образов? Что это за свойство?
б) Чем gVisor/runsc и kata-runtime принципиально отличаются от runc и crun по модели изоляции?
в) В чём разница между runc и crun, если оба — обычные «процессные» рантаймы?

<details>
<summary>Решение</summary>

**а) Взаимозаменяемость через единый формат бандла.** Все low-level рантаймы реализуют один и тот же runtime-spec и принимают на вход одинаковый OCI bundle (`config.json` + `rootfs`). Они подключаются туда же, где ожидается OCI runtime, поэтому сменить runc на crun или runsc можно правкой одной строки в конфигурации high-level рантайма — без изменений в образах и приложениях. Образ и бандл от рантайма не зависят: high-level рантайм распаковывает слои в `rootfs` и генерирует `config.json`, а кто именно исполнит бандл — деталь нижнего уровня.

**б) Модель изоляции:**

- **runc / crun** — запускают целевой процесс как PID 1 контейнера прямо на ядре хоста, создавая namespaces, cgroups, применяя seccomp и capabilities. Контейнеры делят одно ядро хоста.
- **kata-runtime** — запускает каждый контейнер в **облегчённой виртуальной машине с отдельным ядром**.
- **gVisor / runsc** (Google) — перехватывает системные вызовы в **пользовательском пространстве** через собственное ядро-прокси.

Оба последних дают границу безопасности **уровня гипервизора**, оставаясь при этом OCI-совместимыми (подключаются как обычный OCI runtime). Это компромисс между изоляцией и накладными расходами/совместимостью.

**в) runc vs crun:** обе полностью совместимы с OCI runtime-spec и напрямую вызывают системные вызовы Linux (`clone`/`unshare` для namespaces, запись в файловую систему cgroups). Разница в реализации:

- **runc** — эталонная реализация на **Go**; используется в большинстве инсталляций.
- **crun** — реализация на **C** (сообщество/Red Hat): меньше по размеру и быстрее запускается за счёт отсутствия рантайма Go и более лёгкого процесса.

</details>

### Задание 4. Ручной запуск через runc и отладочные CLI

Дан минимальный эксперимент с эталонным рантаймом:

```bash
mkdir -p mycontainer/rootfs
# экспорт ФС образа в rootfs опускаем
cd mycontainer
runc spec                 # ?
runc run mycontainerid    # ?
```

а) Что делает каждая из двух команд `runc`, и какой вывод имеет автор раздела фразой «контейнер — это всего лишь бандл и процесс»?
б) Почему `runc run` обходится без containerd, Docker и Kubernetes — что именно делает за нас вся остальная цепочка?
в) В чём разница между `ctr` и `crictl` и почему оба не предназначены для повседневной эксплуатации?

<details>
<summary>Решение</summary>

**а) Команды runc:**

- `runc spec` создаёт шаблонный `config.json` с разумными значениями по умолчанию (namespaces, базовые mounts, seccomp).
- `runc run mycontainerid` исполняет готовый бандл напрямую.

Бандл состоит ровно из двух частей: обязательного `config.json` в корне каталога и корневой ФС (по умолчанию каталог `rootfs`, путь задаётся полем `root.path`). Имея эти две вещи, low-level рантайм создаёт namespaces, настраивает cgroups, применяет seccomp/capabilities и запускает процесс как PID 1. Отсюда и тезис: для запуска контейнера достаточно бандла и процесса — никаких образов и реестров на этом уровне нет.

**б) Что прячет цепочка.** `runc run` работает напрямую, потому что вся подготовка уже сделана руками: распакован `rootfs` и есть `config.json`. В реальной системе именно это и выполняют высокоуровневые слои:

- high-level рантайм (containerd/CRI-O) делает pull образа, хранит слои через OverlayFS, распаковывает их в `rootfs` и генерирует `config.json`;
- containerd-shim становится родителем процесса и собирает I/O и exit code;
- kubelet через CRI оркеструет всё это.

`runc` ничего не знает об образах, реестрах и сети — он лишь превращает готовый бандл в живой процесс. «Именно эту работу под капотом выполняет вся остальная цепочка».

**в) ctr vs crictl** (оба — инструменты отладки, а не эксплуатации):

| Инструмент | Уровень | Что видит |
|---|---|---|
| `ctr` | containerd | работает поверх containerd напрямую, **не видит метаданные Kubernetes** (образы, контейнеры, задачи) |
| `crictl` | CRI | смотрит через CRI-эндпоинт и показывает поды и контейнеры так, **как их понимает kubelet** |

Для прикладных задач используют `kubectl` или `nerdctl`. Прямой доступ через `ctr`/`crictl` минует слои метаданных и не отражает полную картину состояния, поэтому годится только для отладки и понимания устройства.

</details>
